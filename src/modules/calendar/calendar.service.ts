// import { CalendarRepository } from './calendar.repository';

// export class CalendarService {
//     private repository = new CalendarRepository();

//     async handleAppointment(sender: string, text: string, sock: any) {
//         // לוגיקה מורכבת מול ה-API של Google Calendar או מסד הנתונים
//         console.log(`Processing calendar request for ${sender}`);
//         await sock.sendMessage(sender, { text: "בודק מתי יש זמן פנוי ביומן..." });
//     }
// }